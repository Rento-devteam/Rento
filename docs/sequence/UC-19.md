# UC-19 — Формальный спор

**Статус:** запланировано

```mermaid
sequenceDiagram
    actor Party as Участник
    participant WebApp as Frontend
    participant API as Backend
    participant DB as PostgreSQL
    participant Mod as Moderation

    Party->>WebApp: Открыть спор
    WebApp->>API: POST /bookings/:id/dispute
    Note over API: BookingStatus.DISPUTED<br/>в enum, transition нет
    API->>DB: status=DISPUTED
    Mod->>Mod: рассмотрение
    API->>DB: resolution → COMPLETED | CANCELLED
```

**Сейчас:** отмена — `POST /bookings/:id/cancel`; возврат — `return/confirm` без ветки спора.

Состояния: [state/Dispute.md](../state/Dispute.md).
