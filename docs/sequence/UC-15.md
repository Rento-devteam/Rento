# UC-15 — Возврат вещи

**Статус:** частично (взаимное подтверждение без фото-чеклиста)

```mermaid
sequenceDiagram
    actor Renter as Арендатор
    actor Landlord as Арендодатель
    participant WebApp as Frontend
    participant API as BookingsWorkflow
    participant DB as PostgreSQL

    Note over Renter,DB: Booking status=CONFIRMED

    Renter->>WebApp: Подтвердить возврат
    WebApp->>API: POST /bookings/:id/return/confirm (JWT)
    API->>DB: returnRenterConfirmedAt

    Landlord->>WebApp: Подтвердить возврат
    WebApp->>API: POST /bookings/:id/return/confirm
    API->>DB: returnLandlordConfirmedAt

    alt оба подтвердили
        API->>DB: COMPLETED, settlement PENDING
        API->>API: capture/release (job)
    else только арендатор + deadline
        Note over API: cron auto-confirm landlord
    end
```

Фото при возврате, спор — **запланировано** (UC-14/19). Legacy путь: `/deals/{id}/return/*` → использовать `/bookings/{bookingId}/...`.
