# UC-11 — Персональные рекомендации

**Статус:** запланировано

```mermaid
sequenceDiagram
    actor User as Пользователь
    participant WebApp as Frontend
    participant API as Backend
    participant ES as Elasticsearch

    User->>WebApp: Блок рекомендаций
    WebApp->>API: GET /recommendations/personalized
    Note over API: Маршрут в OpenAPI;<br/>в NestJS не реализован
    API-->>WebApp: listingIds[]
    WebApp-->>User: Карточки
```

**Сейчас:** каталог использует `GET /search` (релевантность / новизна / цена).
