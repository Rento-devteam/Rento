# UC-20 — Блокировка пользователя с низким ARS

**Статус:** запланировано (enum UserStatus BANNED/SUSPENDED; автоматики нет)

```mermaid
sequenceDiagram
    participant Job as Trust / admin job
    participant API as Backend
    participant DB as PostgreSQL

    Job->>API: score < threshold
    API->>DB: User status=BANNED
    API->>DB: cancel active bookings
    API->>DB: hide listings
    Note over API: Админ-API и batch job<br/>не реализованы
```

**Сейчас:** `BANNED`/`SUSPENDED` проверяются при Telegram-auth; массовой блокировки по ARS нет.
