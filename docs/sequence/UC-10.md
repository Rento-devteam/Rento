# UC-10 — Поиск на карте

**Статус:** запланировано

```mermaid
sequenceDiagram
    actor User as Пользователь
    participant WebApp as Frontend
    participant API as Backend
    participant ES as Elasticsearch
    participant Maps as Maps API

    User->>WebApp: Карта + фильтры
    WebApp->>API: GET /search?lat=&lon=&distanceKm=
    Note over API,ES: В коде: geo_distance filter<br/>при lat/lon/distanceKm
    API->>ES: geo query ACTIVE
    ES-->>API: listingIds
    API-->>WebApp: results с координатами
    WebApp->>Maps: отображение маркеров
```

**Сейчас:** backend поддерживает geo-параметры в `GET /search`; отдельного экрана «карта» на фронте может не быть — уточняйте по Figma/репозиторию frontend.

Геокодирование адреса объявления: `POST /geo/geocode` (UC создания объявления).
