# UC-21 — Панель модератора

**Статус:** запланировано

```mermaid
sequenceDiagram
    actor Mod as Модератор
    participant Admin as Admin UI
    participant API as Backend
    participant DB as PostgreSQL

    Mod->>Admin: Очередь жалоб
    Admin->>API: GET /moderation/reports
    Note over API: Нет admin moderation routes

    Mod->>Admin: Заблокировать объявление
    Admin->>API: POST /moderation/listings/:id/block
    API->>DB: Listing BLOCKED
```

**Сейчас:** автомодерация **текста** при create/update/publish (rules + Ollama). Health: `GET /health/moderation-llm`. См. [moderation-draft-ai.md](../moderation-draft-ai.md).
