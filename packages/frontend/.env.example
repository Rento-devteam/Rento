# Copy to .env.local (or .env) for local development.
# Backend API (see packages/backend/.env.example for server-side variables).
VITE_API_BASE_URL=http://localhost:3000
# Optional: override Telegram bot deep link (defaults in code if unset).
# VITE_TELEGRAM_BOT_DEEPLINK=https://t.me/your_bot?start=
