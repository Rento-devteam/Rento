    erDiagram
    ПОЛЬЗОВАТЕЛИ {
    bigint id_пользователя PK
    string фамилия
    string имя
    string отчество
    boolean проверенный_пользователь
    boolean пользователь_онлайн
    datetime последнееВремяОнлайн
    string пароль
    string email
    string номер_телефона
    string роль
    string серия_номер_паспорта
    string фото_аккаунта
    datetime дата_созданияАккаунта
    }

    ИЗБРАННОЕ {
        bigint id_пользователя FK
        bigint id_объявления FK
    }

    ОБЪЯВЛЕНИЯ {
        bigint id_объявления PK
        bigint id_пользователя FK
        datetime датаПубликации
        string описание
        string название
        string город
        decimal широта
        decimal долгота
        decimal ценаЗаЧас
    }

    ЧАТЫ {
        bigint id_чата PK
        bigint id_объявления FK
        datetime времяСоздания
        datetime времяОбновления
    }

    УЧАСТНИКИ_ЧАТА {
        bigint участник_id PK
        bigint id_чата FK
        bigint id_пользователя FK
        string роль
    }

    ФОТОГРАФИИ_ОБЪЯВЛЕНИЙ {
        bigint id_фотографии PK
        string фотография
    }

    СВЯЗЬ_ОБЪЯВЛЕНИЯ_ФОТО {
        bigint id_объявления FK
        bigint id_фотографии FK
    }

    КАЛЕНДАРЬ_ДОСТУПНОСТИ {
        bigint id_доступа PK
        bigint id_объявления FK
        string статусДоступности
        date датаДоступности
        int минимальныйСрок
        int максимальныйСрок
    }

    АРЕНДЫ {
        bigint id_аренды PK
        bigint id_доступа FK
        bigint id_арендатора FK
        datetime времяНачала
        datetime времяКонца
        string статусАренды
        string статусПлаты
        decimal залог
        datetime датаСоздания
    }

    ПОЛЬЗОВАТЕЛИ_ВЕЩИ {
        bigint id_пользователя FK
        bigint id_вещи FK
    }

    %% ------------------- Crow's Foot Relationships -------------------
    ПОЛЬЗОВАТЕЛИ ||--o{ ИЗБРАННОЕ : "добавляет в избранное"
    ОБЪЯВЛЕНИЯ ||--o{ ИЗБРАННОЕ : "попадает в избранное"

    ПОЛЬЗОВАТЕЛИ ||--o{ ОБЪЯВЛЕНИЯ : "создаёт"

    ОБЪЯВЛЕНИЯ ||--o{ ЧАТЫ : "обсуждается в"
    ЧАТЫ ||--o{ УЧАСТНИКИ_ЧАТА : "содержит"
    ПОЛЬЗОВАТЕЛИ ||--o{ УЧАСТНИКИ_ЧАТА : "участвует как"

    ОБЪЯВЛЕНИЯ ||--o{ СВЯЗЬ_ОБЪЯВЛЕНИЯ_ФОТО : "имеет"
    ФОТОГРАФИИ_ОБЪЯВЛЕНИЙ ||--o{ СВЯЗЬ_ОБЪЯВЛЕНИЯ_ФОТО : "используется в"

    ОБЪЯВЛЕНИЯ ||--o{ КАЛЕНДАРЬ_ДОСТУПНОСТИ : "расписание"

    КАЛЕНДАРЬ_ДОСТУПНОСТИ ||--o{ АРЕНДЫ : "бронируется"
    ПОЛЬЗОВАТЕЛИ ||--o{ АРЕНДЫ : "арендует как"

    ПОЛЬЗОВАТЕЛИ ||--o{ ПОЛЬЗОВАТЕЛИ_ВЕЩИ : "владеет"